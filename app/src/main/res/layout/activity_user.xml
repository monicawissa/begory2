<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="userViewModel"
            type="com.attendance.myproject.begory.presentationLayer.main.setting.userExist.UserViewModel"
            />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/rect_dialog_box">

        <TextView
            android:id="@+id/d_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:gravity="center"
            android:text="@{userViewModel.mTitleTV}"
            android:textSize="24sp"
            android:background="@color/primaryColor"
            android:textColor="@color/white"
            android:textStyle="bold" />

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_marginTop="4dp"
            android:layout_marginStart="4dp"
            android:layout_alignParentStart="true"
            android:layout_gravity="center_horizontal|top"
            android:indeterminate="true"
            android:indeterminateDrawable="@drawable/progress_bar_black"
            android:visibility="@{userViewModel.dataLoading ? View.VISIBLE: View.GONE  }"
            />
        <androidx.core.widget.NestedScrollView
            android:id="@+id/user_exist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/d_title"
            >

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:orientation="vertical"
                >


                <EditText
                    android:id="@+id/et_checkmobile"
                    android:layout_width="match_parent"
                    android:layout_height="42dp"
                    android:background="@drawable/rect_white_box"
                    android:hint="@string/mobile"
                    android:text="@={userViewModel.mobile}"

                    android:layout_marginTop="8dp"
                    android:imeOptions="actionNext"
                    android:layout_marginLeft="16dp"
                    android:layout_marginRight="16dp"
                    android:inputType="phone"
                    android:gravity="end|right"
                    android:maxLines="1"
                    android:padding="12dp"
                    android:textColor="#3F3F3F"
                    android:textColorHint="#3F3F3F"
                    android:textSize="14sp"
                    />

                <Button
                    android:id="@+id/btn_check"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:background="@drawable/btn_white_selector"
                    android:paddingLeft="16dp"
                    android:paddingRight="16dp"
                    android:paddingTop="4dp"
                    android:paddingBottom="4dp"
                    android:layout_gravity="center_horizontal"
                    android:text="@string/check"
                    android:textColor="@color/black"
                    android:textSize="16sp"
                    android:clickable="@{userViewModel.isCheckBtnAvailable()}"

                    android:onClick="@{()-> userViewModel.check()}"

                    android:textStyle="bold" />

            </LinearLayout>

        </androidx.core.widget.NestedScrollView>


    </RelativeLayout>
</layout>